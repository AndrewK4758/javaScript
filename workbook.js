const workbook = (n, k, arr) => {
    
    let page = 1;
     

    for (let problem = 0; problem < arr.length ; problem++) {
        console.log(Math.floor(arr[4] / k))
    }
};

console.log(workbook(5, 3, [4, 2, 6, 1, 10]))

/*
n = chapters
k = problems per page
arr = problems per chapter
*/